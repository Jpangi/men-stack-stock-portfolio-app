<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <nav id="navBar">
        <a class="btn" href="/">Home</a>
        <% if(user) { %>
            <a class="btn" href="/users/<%=user._id%>">Portfolio</a>
            <a class="btn" href="/stocks">My Stocks</a>
            <a class="btn" href="/stocks/new">New Stock</a>
            <a class="btn" href="/auth/logout">Logout</a>
            <% } else { %>
                <a class="btn" href="/auth/sign-in">Sign In</a>
                <a class="btn" href="/auth/sign-up">Sign Up</a>
                <% } %>
    </nav>
</body>

</html>